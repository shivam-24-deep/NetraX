node_modules/
dist/
dist-ssr/
.env
.env.local
*.local

# Local Gmail OAuth tokens / seen-message baseline (server/gmail-client.ts) — never committed.
server/.gmail-tokens.json
server/.gmail-seen.json

.claude/

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
Thumbs.db

*.tsbuildinfo

ml/data/*.csv
ml/data/*.zip
ml/.venv/
ml/.pytest_cache/
__pycache__/
*.pyc

supabase/.temp/

# Raw/processed datasets: large, reproducible via scripts/data/download_datasets.py,
# never committed. Only data/README.md, data/samples/, and data/demo/ are tracked.
data/raw/
data/processed/
data/splits/

